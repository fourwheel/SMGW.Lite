#pragma once
// ---------------------------------------------------------------------------
// WiFi credentials for zero-touch deployment.
// Copy this file to wifi_credentials.h and fill in your values.
// wifi_credentials.h is listed in .gitignore and must NEVER be committed.
// ---------------------------------------------------------------------------
#define WIFI_DEFAULT_SSID        "YourSSID"
#define WIFI_DEFAULT_PASSWORD    "YourPassword"
#define WIFI_DEFAULT_AP_PASSWORD "YourAPPassword"   // min. 8 characters
